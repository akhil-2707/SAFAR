# S.A.F.A.R. Backend Configuration (Example)
PORT=5000
NODE_ENV=development

# Optional: MongoDB Connection URI
# If unset, S.A.F.A.R. runs with its zero-dependency in-memory mock store
MONGO_URI=

# JWT Secret for local dev authentication
JWT_SECRET=safar_jwt_dev_secret_replace_in_production
